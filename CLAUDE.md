# 手順帳（STEP CARDS）開発ガイド

## 北極星（このプロジェクトの絶対基準）
- **誰でも一発で手順を登録できる**こと。機械オンチの人が迷わず使えるUIを常に最優先する。
- **シンプルさは"思想"であって妥協ではない**。機能を足すより、足さない判断を尊ぶ。
- **アカウント不要・完全オフライン・端末内完結**を死守する。これがこのアプリ唯一の差別化軸。

## やらないこと（差別化を壊す行為。提案も不要）
- クラウド同期・ログイン・アカウント・SNS共有機能を足さない。
- 外部サービス（Firebase等）への依存を足さない。データは端末から出さない。
- 「業務マニュアルツール」的な高機能化（権限管理・コメント・承認フロー等）に寄せない。
- 過度な設定項目・モード・カスタマイズを増やさない。迷いを生むものは原則却下。

## 技術の土台（後から変えると高コストな部分。勝手に変更しない）
- 単一HTML＋Canvas/DOM、ビルドレス。外部ライブラリは原則持ち込まない（必要時は理由を提示し承認を得る）。
- 永続化は **IndexedDB**。`localStorage`は使わない（容量・画像に不向き）。
- 写真は **Blob** で保存し `URL.createObjectURL` で描画。base64は移行時のみ。
- データには `schema`（整数バージョン）を持たせる。構造変更時はマイグレーションを書く。
- エクスポート/インポートは **JSON1ファイル**（写真base64同梱）。これがバックアップ兼デバイス間移行の唯一の手段。

## データ保全（最重要）
- ユーザーが登録した手順・写真・実施ログは**絶対に失わせない**。
- 破壊的操作（削除・上書き・インポートでの置換）は必ず確認ダイアログを挟む。
- マイグレーション前に自動バックアップ（または失敗時ロールバック）を用意する。

## 応答・作業の方針
- 日本語で、結論から言い切る。前置きの賞賛や保険は不要。
- 変更は**差分（diff）で報告**。何をなぜ変えたかを簡潔に。触っていない箇所は触らない。
- 推測で実装しない。仕様が曖昧なら手を止めて質問する。
- 1フェーズ＝1まとまり。フェーズをまたいで勝手に先走らない。
- 不確実・未確認なことは「未確認」と明示する。盛らない。

## Git運用
- プロトタイプ段階は main へ直コミットで可。
- 破壊的・リスクのある変更（IndexedDBマイグレーション、削除機能等）はブランチを切る。
- 各フェーズ完了時にコミット。コミットメッセージにフェーズ番号を入れる。
