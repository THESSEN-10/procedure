# 手順帳（STEP CARDS）要件定義

## 1. コンセプト
写真＋手順をシンプルに登録・閲覧できる、汎用の「やり方カード」アプリ。料理・掃除・家事・園芸盆栽・ものづくり、何にでも使える。機械オンチの人でも一発で登録できることを最優先する。

## 2. 北極星（絶対基準）
- 誰でも迷わず手順を登録できる。
- シンプルさは思想。機能を足すより足さない判断を尊ぶ。
- アカウント不要・完全オフライン・端末内完結。これが唯一の差別化軸。

## 3. 対象ユーザー
個人（趣味の手順を残したい人）、または一人〜数人の小規模な店・現場。新人や家族など「やり方を知らない人」に渡して使ってもらう場面も含む。

## 4. スコープ
### 入れる
- 手順の登録・閲覧・編集・削除・複製
- 写真1枚＋番号付き手順、用意するもの、注意・コツ、カテゴリ、場所/道具メモ、所要時間
- 数字の自動チップ強調（個数・分量の誤読防止）
- 手順ごとのチェックオフ（進捗表示）と「やった」実施記録（任意）
- テンプレート（組み込みプリセットから新規をプリフィル）
- IndexedDB永続化、JSONエクスポート/インポート、印刷/PDF出力
- 手順単位／全体のファイル共有（Web Share・OS共有シート経由。サーバー不要）
- PWA（オフライン動作・ホーム画面追加対応）

### 入れない（差別化を壊すため、提案も不要）
- クラウド同期・ログイン・アカウント・SNS共有
- 外部サービス（Firebase等）依存／データを端末外へ送る機能
- 広告（オフライン・プライバシー思想と衝突）
- 業務ツール的高機能化（権限・コメント・承認フロー等）
- ライブ同期・共有ライブラリ・自動同期（共有はあくまでファイル経由の手動）
- 過剰な設定・モード・カスタマイズ

## 5. データモデル
1手順 = item:
- `id`, `schema`(整数), `name`, `cat`, `place?`, `time?`
- `photo`：**base64で保存・エクスポートと形式統一**。保存前に必ずリサイズ/圧縮する（長辺〜1280px・JPEG品質80%目安）。フル解像度のまま持たない。
- `tools[]`, `cautions[]`, `steps[{body}]`
- `log[]`（実施日の配列）, `createdAt`, `updatedAt`, `templateId?`
- `extra{}`：任意のキー値マップ。将来の製造寄りフィールド（図番・工程No.・員数等）をスキーマ破壊なしに後付けするための受け皿。コア機能では未使用。
- チェック進捗は永続化しない（ランタイムのみ）

アプリ全体に課金状態 `tier`（`free` / `plus`）を1箇所で保持。機能はこのフラグでゲートし、条件分岐を散らさない（将来 `pro` 追加を見込む継ぎ目）。

## 6. バックアップ／復旧戦略（サーバーなし）
「アプリはバックアップ"ファイル"を作るだけ。保管はユーザー自身のOS/クラウドが担う」。
- JSONエクスポート（写真base64同梱の1ファイル）＝完全バックアップ兼デバイス間移行手段。
- 保存先はユーザーの iCloud Drive / Google Drive / Files 等（共有シート or File System Access API経由）。
- `navigator.storage.persist()` を起動時に要求し、ストレージ消去リスクを下げる（特にiOS Safari対策）。
- 「前回バックアップから30日経過」程度の軽いリマインド。
- 印刷/PDFは人間可読の副次バックアップ＆現場貼り出し用。

## 7. 配信
- 第一目標：PWA（ブックマーク／ホーム画面追加で動く）。インストールは"おまけ"、URLでも完全動作が前提。
- iOSはホーム画面追加が手動・低発見性、AndroidはInstallボタンで促せるがブラウザ差あり、と理解した上で割り切る。
- 多数配布・発見性が必要になった場合のみ、Capacitorで現コードをラップしてストア配信を検討（Apple $99/年、Google $25一回、Google個人はテスター12人/14日が必要）。

## 8. 収益化スタンス（決定）
- モデル：**基本無料＋一回課金アンロック**（フリーミアム、サーバー不要）。
- 無料枠は気前よく（手順10件程度まで無料。ライトユーザーが上限に当たらない設計）。`plus` 購入で登録数上限を解除。
- ⚠️ **バックアップ・エクスポートは絶対に課金の壁の向こうに置かない**。データ保全は無料で守る（北極星「データを失わせない」と直結）。課金で売るのは"数"と"便利さ"であって"安全"ではない。
- 課金状態の判定は端末ローカル（抜け道は許容。ガチガチのDRMは組まない＝シンプルさ優先）。
- サブスク・広告は不採用。
- 有料配布時はサンプル手順・画像をすべて自作/正規ライセンスに差し替えること。

## 8.5 将来の製造寄りオプション（継ぎ目のみ用意、実装は後）
- **足してよい（オンデバイス完結＝思想を保てる／`pro`候補）**：員数強調、標準作業票風PDF出力、複数写真・工程内サブ手順、改訂メモ（端末内）、QRで特定手順を開く、CSV/JSON構造化エクスポート。
- **足さない（サーバー必須＝別製品へのフォーク。"オプション"扱い禁止）**：多人数の中央管理、社員横断の閲覧トラッキング/レポート、承認ワークフロー、クラウド同期。

## 9. 残課題
- [x] 写真の保存形式 → base64統一＋保存前リサイズ/圧縮（§5）で確定。
- [x] 収益化 → 基本無料＋一回課金アンロックで確定（§8）。
- [ ] 配信の最終ゴール：PWAのみ か、いずれストア配信も視野に入れるか（後決め可。継ぎ目はCapacitorラップ前提）。
- [ ] 無料枠の具体ライン（10件で妥当か、運用しながら調整）。

## 10. 開発フェーズ
- **Phase 0** 土台移行（リポジトリ化・CLAUDE.md・git・動作確認）
- **Phase 1** IndexedDB永続化＋`storage.persist()`要求。写真はbase64保存＋リサイズ/圧縮。`tier`フラグ層と`extra`フィールドの継ぎ目も同時に用意（中身の課金UIはまだ作らない）
- **Phase 2** 削除＋複製（破壊的操作は確認ダイアログ必須）
- **Phase 3** JSONエクスポート/インポート ＋ 印刷/PDF出力（※エクスポートは常に無料）
- **Phase 4** テンプレート（組み込みプリセットでプリフィル）
- **Phase 5** PWA化（manifest＋Service Worker、Android用Installボタン、iOS用の追加ヒント）
- **Phase 6** フリーミアム（無料枠の上限判定＋`plus`アンロックの一回課金）
- 以降オプション：並べ替え、複数写真、検索強化、チェックリスト型（`type`トグル。再訪頻度が利点／識別子のボケに注意。MVPを使ってから判断）、製造寄り`pro`（§8.5の「足してよい」側のみ）
