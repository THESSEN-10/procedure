# 開発用スクリーンショット・検証ツール（端末内のみ。GitHub等に上げない）
.shots/

# 依存（必要なら各自インストール）
node_modules/

# サンプル写真の元ファイル（index.htmlにbase64で内包済み。元PNGは重いので除外）
# ルート直下の画像のみ対象。アプリで使う icons/*.png は対象外（サブフォルダなので除外されない）
/*.png

# OS・エディタ
.DS_Store
Thumbs.db
